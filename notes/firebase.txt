yarn add firebase
* services of authentication are the same for the app, but the providers can be different.

* Firestore: collection <- document <- data (ex: collection Users)

* await getRedirectResult(auth); (gets data when the app comes back from redirect login)

